# Copy to deploy/.env and replace every placeholder before starting.
POSTGRES_DB=forgejo_mcp
POSTGRES_USER=forgejo_mcp
POSTGRES_HOST_PORT=5433
FMCP_HTTP_PORT=8000
FMCP_BIND_ADDRESS=127.0.0.1
FMCP_LOG_LEVEL=INFO
FMCP_LOG_FORMAT=json
FMCP_ENVIRONMENT=production
# Set false only for direct localhost testing. Use true behind production HTTPS.
FMCP_COOKIE_SECURE=false
# Keep false in production. Set true only when using the local test-forgejo profile.
FMCP_ALLOW_INSECURE_FORGEJO_HTTP=false
FMCP_ALLOW_UNVERIFIED_FORGEJO_TLS=false
# Required in production. JSON list; include the exact scheme, host, port and subpath.
FMCP_FORGEJO_ALLOWED_BASE_URLS=["https://git.example.com"]
# Leave empty unless a known reverse proxy forwards client addresses; never use 0.0.0.0/0.
FMCP_TRUSTED_PROXY_CIDRS=[]
# Keep false unless repository migrations must reach an explicitly trusted private source.
FMCP_MIGRATION_ALLOW_PRIVATE_HOSTS=false
# Browser-based MCP clients require an exact origin here. Non-browser clients omit Origin.
FMCP_MCP_ALLOWED_ORIGINS=[]
FMCP_CREDENTIAL_ENCRYPTION_KEY_VERSION=1
FMCP_MCP_REQUEST_MAX_BYTES=2097152
FMCP_COMMIT_MAX_FILES=100
FMCP_COMMIT_MAX_TOTAL_BYTES=10485760
FMCP_FORGEJO_CONNECT_TIMEOUT_SECONDS=5
FMCP_FORGEJO_READ_TIMEOUT_SECONDS=30
FMCP_FORGEJO_WRITE_TIMEOUT_SECONDS=30
FMCP_FORGEJO_POOL_TIMEOUT_SECONDS=5
FMCP_FORGEJO_SAFE_RETRY_ATTEMPTS=2
FMCP_MCP_RATE_LIMIT_WINDOW_SECONDS=60
FMCP_MCP_TOKEN_RATE_LIMIT_REQUESTS=120
FMCP_MCP_USER_RATE_LIMIT_REQUESTS=240
# Optional OAuth 2.1 authorization-code flow for browser-capable MCP clients.
FMCP_OAUTH_ENABLED=false
# When enabled, use the public HTTPS origin serving this application.
FMCP_OAUTH_ISSUER_URL=https://forge-mcp.example.com
FMCP_OAUTH_RESOURCE_URL=https://forge-mcp.example.com/mcp
# Exact HTTPS origins whose client metadata documents may be fetched (CIMD).
FMCP_OAUTH_CIMD_ALLOWED_ORIGINS=[]
FMCP_OAUTH_ACCESS_TOKEN_TTL_SECONDS=3600
FMCP_OAUTH_REFRESH_TOKEN_TTL_DAYS=30
FMCP_OAUTH_REQUEST_MAX_BYTES=65536
FMCP_SHUTDOWN_GRACE_PERIOD_SECONDS=30
FMCP_BOOTSTRAP_ADMIN_USERNAME=admin
# Optional absolute host paths; defaults are files under deploy/secrets/.
# FMCP_ADMIN_PASSWORD_FILE=/secure/path/admin_password
# FMCP_CREDENTIAL_KEY_FILE=/secure/path/credential_key
# FMCP_DATABASE_URL_SECRET_FILE=/secure/path/database_url
# FMCP_POSTGRES_PASSWORD_FILE=/secure/path/postgres_password
FORGEJO_TEST_HTTP_PORT=3000
